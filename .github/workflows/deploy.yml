name: Deploy

on:
    workflow_call: 
    workflow_dispatch: 
jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Mock deployment
              run: echo "Deploying to production..."
